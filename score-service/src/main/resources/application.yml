server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:8901/user

logging:
  level:
    org.springframework: DEBUG