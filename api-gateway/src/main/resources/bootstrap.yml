spring:
  application:
    name: api-gateway
  profiles:
    active: default
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server  # 配置服务器
  output:
    ansi:
      enabled: ALWAYS

logging:
  level:
    org.springframework: INFO
  file: C:\ylong\workspace\IntelliJProjects\spring-cloud-stack\api-gateway\api-gateway.log